# Minimal config for GazeboSimROS2ControlPlugin initialization
# Only contains update_rate - all other params come from spawner --param-file
controller_manager:
  ros__parameters:
    update_rate: 500

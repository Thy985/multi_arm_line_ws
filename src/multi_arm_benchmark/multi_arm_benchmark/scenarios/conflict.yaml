name: conflict
description: Resource conflict scenario — both arms compete for same zone
tasks:
  - task_id: conflict_left_arm_zone_a
    arm_name: left_arm
    action_type: move
    zone_name: zone_a
    position_name: ready
    timeout: 30.0
  - task_id: conflict_right_arm_zone_a
    arm_name: right_arm
    action_type: move
    zone_name: zone_a
    position_name: ready
    timeout: 60.0